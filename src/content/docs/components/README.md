# Component Documentation Templates

- Use `_template.md` as the baseline for new component docs generated via `component-docs` prompts.
- Ensure frontmatter fields remain consistent so automation can track `promptVersion`, `lastPromptRun`, and `demoComponent` references.
- Store generated Markdown alongside any component-specific assets or callouts.
